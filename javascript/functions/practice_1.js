let person = {
    my_name : "Micheal Scott",
    age : 44,
    company : "dunder mifflin"
}

let result =  person.my_name + " is " + person.age + " years old and works at " + person.company

function logData(){
     console.log(result)
}

logData()